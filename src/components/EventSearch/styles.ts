import styled from "@emotion/styled";

export const SearchOverlay = styled.div`
    top: 68px;
    position: fixed;
    left: 0px;
    bottom: 0px;
    display: flex;
    align-items: center;
    width: 100vw;
    height: 100vh;
    min-height: 100%;
    min-width: 100%;
    background: rgba(51, 51, 51, 0.75);
    z-index: 3;
`